# POST.md — <skill-name>

<!--
X-thread-shaped explainer for this skill. ~200 words. Fill in every
placeholder before publishing. Ship this alongside SKILL.md so anyone
who reads the thread can install the skill and reproduce the result.

Voice: plain, concrete, no hype. Show the failure mode, then the fix.
Numbers > adjectives. One idea per beat.
-->

**Beat 1 — hook (1 line).**
new loopkit skill: `<skill-name>` — <one-line-what-it-does>.

**Beat 2 — the failure mode (2–3 lines).**
without it, the agent <before-example>. every session. the failure isn't
loud, which is why it slips past review.

**Beat 3 — the fix (2–3 lines).**
the skill forces <the alternative behaviour>. `<invocation>` (or auto-loads
when the trigger phrase matches). the checklist inside SKILL.md is short
on purpose — it has to fit in every context window it lands in.

**Beat 4 — the receipt (2–3 lines).**
same prompt, skill loaded: <after-example>. before/after diff lives in
`skills/<skill-name>/evidence/`. no eval theatre — one real task, both
transcripts, judge for yourself.

**Beat 5 — why it matters (1–2 lines).**
<why-this-matters>. small skill, compounding effect across a long run.

**Beat 6 — CTA (1 line).**
<call-to-action> — repo: github.com/Archive228/loopkit. drop it in your
`.claude/skills/` and try.
